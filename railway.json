{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "until php artisan migrate:fresh --force; do echo 'Waiting for database...'; sleep 5; done && php artisan db:seed --force --class=AdminSeeder && PHP_CLI_SERVER_WORKERS=4 php artisan serve --host=0.0.0.0 --port=${PORT:-8080}"
    }
}
